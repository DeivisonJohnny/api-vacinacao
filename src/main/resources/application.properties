spring.application.name=api-vacinacao
# URL de conex�o com o banco de dados
spring.datasource.url=jdbc:mysql://127.0.0.1:3307/devjohnny_vacinas?useSSL=false

# Nome de usu�rio e senha
spring.datasource.username=root
spring.datasource.password=password

# Driver de conex�o
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Habilitar o uso do JPA
spring.jpa.hibernate.ddl-auto=update

# Pode ser 'create', 'create-drop', 'update', 'validate'
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

server.port=6060

# Ativar depura��o
spring.devtools.restart.enabled=true

api.security.token.secret=${JWT_SECRET:my-secret-key}

debug=true
